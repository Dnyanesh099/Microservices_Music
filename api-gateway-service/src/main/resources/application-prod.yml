# ==========================================
# This is Dummy Data for Prod server how it is worked okkk
# ==========================================

spring:
  application:
    name: api-gateway-service

  kafka:
    bootstrap-servers:
      - b-1.msk-prodcluster.abcd.kafka.us-east-1.amazonaws.com:9092
      - b-2.msk-prodcluster.abcd.kafka.us-east-1.amazonaws.com:9092

  redis:
    host: sonymusic-prod-redis.abcd.use1.cache.amazonaws.com
    port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://discovery-prod:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway,prometheus

  tracing:
    sampling:
      probability: 1.0

  zipkin:
    tracing:
      endpoint: https://xray.us-east-1.amazonaws.com
